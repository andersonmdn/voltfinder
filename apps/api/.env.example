# Configurações da API VoltFinder
PORT=3000
NODE_ENV=development

# JWT Secret - ALTERE EM PRODUÇÃO!
JWT_SECRET=your-super-secret-jwt-key-here

# Configuração do Banco PostgreSQL (Docker)
# Para Docker Compose ou containers locais na porta 5433
DATABASE_URL="postgresql://postgres:sql@localhost:5433/voltfinder?schema=public"

# Configurações alternativas para diferentes ambientes:
# DATABASE_URL="postgresql://postgres:sql@voltfinder-postgres:5432/voltfinder?schema=public"  # Se a API rodar em container
# DATABASE_URL="postgresql://username:password@localhost:5432/voltfinder?schema=public"      # PostgreSQL local padrão

# Configurações de CORS (opcional)
# CORS_ORIGIN=http://localhost:3000,http://localhost:19006

# Configurações de Log (opcional)
# LOG_LEVEL=info